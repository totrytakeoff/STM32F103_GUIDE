# 60_freertos_adc_dma - FreeRTOS ADC DMA 采样

## 1. 本课到底在学什么

本课表面现象是：PA0 模拟电压被 ADC1 连续采样，DMA 自动把 16 个采样值搬到 `g_adc_buf`，DMA 半满或全满中断通知 `adc_task`，任务计算平均值，大于 2048 时点亮 PC13，否则熄灭。

真正要学的是 ADC、DMA、任务通知三层协作。CPU 不再轮询每一次 ADC 转换，而是让 DMA 搬运数据，让中断只通知事件，让任务处理平均值和 LED 输出。

本课继续按六层拆解：现象层看板子和串口输出，硬件层看引脚和连接，芯片模块层看 ADC/DMA/UART/GPIO/NVIC，寄存器层看控制位和状态位，C/CMSIS 层看源码语句，HAL/工程层看结构体字段、回调和返回值；FreeRTOS 部分还要解释任务、队列、通知、阻塞和 ISR 边界。

## 2. 本课学习目标

- 能说出现象来自哪条执行链路。
- 能解释每个新对象属于硬件层、寄存器层还是 RTOS 层。
- 能把寄存器版语句和 HAL 字段对应起来。
- 能根据具体现象排查时钟、GPIO、外设、DMA、中断和任务。
- 能说明 ISR 里为什么只做短动作。
- 能说明任务为什么阻塞等待而不是轮询。
- 能看懂源码里的缓冲区、句柄和返回值风险。
- 能把本课和前后课程的边界分清楚。

## 3. 本课目录结构

```text
60_freertos_adc_dma/
├── README.md
├── reg/
│   ├── platformio.ini
│   └── src/main.c
└── hal/
    ├── platformio.ini
    └── src/main.c
```

两个版本都要读。寄存器版让你看到 RCC、GPIO、ADC、DMA、USART 等寄存器怎样配合；HAL 版让你看到同一件事如何变成 handle、Init 结构体、回调和封装 API。

## 4. 实验硬件与工程前提

- PA0：ADC1_IN0 模拟输入。
- PC13：平均值阈值指示 LED。
- ADC 时钟：72MHz / 6 = 12MHz。
- DMA1_Channel1：ADC1 固定映射通道。
- 缓冲区：`g_adc_buf[16]`，半满 8 点，全满 16 点。
- DMA IRQ 优先级 6，可调用 FromISR API。

## 5. 先建立一个最基本的脑图

```text
PA0 电压 -> ADC1 连续转换 -> ADC1->DR
  -> DMA1_Channel1 搬到 g_adc_buf[16]
  -> HTIF1/TCIF1 中断
  -> vTaskNotifyGiveFromISR(adc_task)
  -> adc_task 求平均值
  -> 平均值和 2048 比较
  -> PC13 输出状态变化
```

先用这张图记住数据从哪里来、经过哪个中断或任务、最后落到哪个输出。后面所有寄存器和 API 都应该能放回这张图里。

## 6. 先认识本课里出现的核心名词

### 6.1 `ADC1` 是什么

ADC1 是模数转换外设，把 PA0 电压转换成 0-4095 的 12 位数字。
本课只用规则序列第 1 个通道 ADC_CHANNEL_0。

### 6.2 `DMA1_Channel1` 是什么

它是 ADC1 在 STM32F1 上固定使用的 DMA 通道。
通道选错时 ADC 转换了也不会搬到缓冲区。

### 6.3 `g_adc_buf[16]` 是什么

它是 DMA 写入的采样缓冲区。
16 个 halfword 对应 16 次 ADC 转换结果。

### 6.4 `HTIF1` 是什么

半传输标志，表示缓冲区前半已经填好。
ISR 清标志后通知任务。

### 6.5 `TCIF1` 是什么

全传输标志，表示整个缓冲区填好。
循环模式下 DMA 随后回到开头继续搬。

### 6.6 `CIRC` 是什么

DMA 循环模式。
没有循环模式时搬完 16 个数据就停。

### 6.7 `MINC` 是什么

内存地址递增。
它让 DMA 依次写入数组不同元素。

### 6.8 `PSIZE/MSIZE` 是什么

外设和内存数据宽度。
ADC DR 是 16 位有效承载 12 位结果，所以本课用 halfword。

### 6.9 `ADC_CR2_DMA` 是什么

它允许 ADC 转换结果触发 DMA 请求。
不打开时 DMA 不会跟着 ADC 工作。

### 6.10 `ADC_CR2_CONT` 是什么

连续转换模式。
它让 ADC 不断采样 PA0。

### 6.11 `ADCPRE_DIV6` 是什么

ADC 预分频为 6，72MHz APB2 分到 12MHz。
F1 ADC 时钟不能超过规格上限。

### 6.12 `CAL` 是什么

ADC 校准位。
上电后校准能减少偏差。

### 6.13 `vTaskNotifyGiveFromISR` 是什么

ISR 中给任务发送通知的 API。
DMA 中断不能直接做长时间求平均。

### 6.14 `ulTaskNotifyTake` 是什么

adc_task 阻塞等待 DMA 事件。
收到通知后才处理缓冲区。

### 6.15 `2048` 阈值是什么

12 位 ADC 中间值约为 4096 的一半。
输入电压高于约 Vref/2 时 LED 状态改变。

## 7. 寄存器版代码逐步讲解

### 7.1 系统时钟和 ADC 分频

寄存器版在 RCC_CFGR 里同时配置 PLL 和 ADCPRE_DIV6。
这保证 ADC_CLK 为 12MHz。

### 7.2 PA0 模拟输入

清 MODE0/CNF0 让 PA0 成为模拟输入。
模拟输入关闭数字输入缓冲，适合 ADC。

### 7.3 DMA CPAR/CMAR

CPAR 指向 `&ADC1->DR`，CMAR 指向 `g_adc_buf`。
方向由外设到内存。

### 7.4 DMA CNDTR

CNDTR 写 16，表示一轮搬运 16 个 halfword。
循环模式会自动重装。

### 7.5 DMA CCR

MINC、CIRC、PSIZE、MSIZE、HTIE、TCIE 共同决定搬运和中断。
先配置再使能。

### 7.6 ADC 序列

SQR1=0 表示 1 个转换，SQR3=0 表示通道 0。
SMPR2 设置采样时间。

### 7.7 ADC 校准

先 ADON 唤醒，再 CAL 等待清零。
没有校准会增加偏差。

### 7.8 DMA 中断

ISR 检查 HTIF1/TCIF1，写 IFCR 清标志。
清标志后通知任务。

### 7.9 任务求平均

adc_task 对 16 个样本求和除以长度。
平均值减少单点抖动。

### 7.10 LED 输出

平均值大于 2048 写 BRR 点亮 PC13，否则写 BSRR 熄灭。
这是采样结果的可见证据。

## 8. HAL 版代码逐步讲解

### 8.1 HAL ADC handle

`hadc1.Instance=ADC1` 绑定 ADC1。
Init 字段配置连续转换、软件触发和右对齐。

### 8.2 HAL DMA handle

`hdma_adc1.Instance=DMA1_Channel1`。
Direction、MemInc、Alignment、Mode 对应寄存器版 DMA CCR/地址配置。

### 8.3 __HAL_LINKDMA

把 ADC handle 和 DMA handle 关联。
没有关联时 HAL_ADC_Start_DMA 不知道用哪个 DMA。

### 8.4 HAL_ADC_ConfigChannel

配置 ADC_CHANNEL_0、Rank 1、采样时间 239.5 cycles。
对应 SQR3 和 SMPR2。

### 8.5 HAL_ADC_Start_DMA

启动 ADC 和 DMA。
任务开头调用一次，后续 DMA 循环搬运。

### 8.6 HAL_ADC_ConvHalfCpltCallback

HAL 半满回调。
内部通知 adc_task。

### 8.7 HAL_ADC_ConvCpltCallback

HAL 全满回调。
同样通知 adc_task。

### 8.8 DMA IRQHandler

DMA1_Channel1_IRQHandler 调 HAL_DMA_IRQHandler。
HAL 再分发到半满/全满回调。

## 9. 两个版本真正应该怎么学

寄存器版和 HAL 版做的是同一个系统。寄存器版适合看清时钟、引脚模式、状态标志、DMA 通道映射和中断清标志；HAL 版适合看清工程结构、handle 关联、回调入口和错误处理。

不要只背 API 名称。每看到一个 API，都要问它最终配置了哪个外设、影响了哪个数据流、失败后现象是什么。这样读，后面遇到综合项目才不会散。

## 10. 检验问题清单

### 10.1 为什么用 DMA？

**答**：避免 CPU 等待每次 ADC 转换，让采样搬运自动进行。

### 10.2 为什么半满也通知？

**答**：半满说明前 8 个样本已可处理，能降低延迟。

### 10.3 为什么平均 16 个样本？

**答**：平均能减小抖动，比单点阈值稳定。

### 10.4 ADC 时钟为什么分频？

**答**：F1 ADC 有最大时钟限制，72MHz 不能直接喂给 ADC。

### 10.5 为什么 ISR 不求平均？

**答**：求平均是较长逻辑，应放任务里，ISR 只通知。

### 10.6 HAL 版为什么要 LINKDMA？

**答**：HAL 需要通过 handle 找到关联 DMA。

### 10.7 阈值 2048 代表什么？

**答**：12 位 ADC 中间值，约等于参考电压的一半。

### 10.8 DMA 通道能换吗？

**答**：ADC1 在 F1 上固定映射到 DMA1_Channel1。

### 10.9 PC13 不动先查什么？

**答**：查 PA0 电压、ADC/DMA 是否启动、DMA 中断是否触发、任务是否收到通知。

### 10.10 缓冲区为什么 volatile？

**答**：寄存器版 DMA 和 CPU 都访问它，volatile 避免编译器错误假设。

## 11. 工程实现步骤

### 11.1 需求分析

先定义输入、处理和输出，不急着写代码。输入可能是 ADC、按键、串口或 DMA；处理可能在任务或回调；输出可能是 LED、串口日志或显示屏。

### 11.2 硬件核查

确认引脚、接线、电源、共地、模拟输入范围和串口参数。ADC 输入不能超过供电范围，串口 TX/RX 要交叉，LCD 还要确认模式脚和对比度。

### 11.3 寄存器路线

按 RCC 时钟、GPIO 模式、外设参数、DMA/NVIC、任务或主循环的顺序搭起来。每一步都能对应到源码里的具体寄存器或宏。

### 11.4 HAL 路线

用 HAL 结构体表达同样的硬件选择，再通过 Init、Start、Callback 把运行链路串起来。HAL 版也要检查返回值和句柄关联。

### 11.5 工程思维

中断和 DMA 只负责搬运或通知，复杂计算、打印、解析、显示刷新放在任务或主循环里。共享数据要明确谁写谁读。

### 11.6 常见工程陷阱

常见问题包括 include 路径错、对象创建失败不查、DMA 通道映射错、中断优先级不合规、串口波特率不匹配、HAL 回调没重启接收、缓冲区长度和协议长度不一致。

## 12. 运行现象

本课表面现象是：PA0 模拟电压被 ADC1 连续采样，DMA 自动把 16 个采样值搬到 `g_adc_buf`，DMA 半满或全满中断通知 `adc_task`，任务计算平均值，大于 2048 时点亮 PC13，否则熄灭。

### 12.1 六层对应关系

现象不是凭空出现的：硬件引脚提供输入输出，芯片外设采样或传输，寄存器决定模式，C 代码组织流程，HAL 封装工程写法，RTOS 层决定任务何时运行。

### 12.2 推荐断点

在初始化结束处、ISR 或回调入口、任务解除阻塞处、最终输出函数处打断点。四个点能把问题分成初始化失败、数据没到、中断没触发、任务没醒、输出没执行。

### 12.3 配置核查

重点核查 `platformio.ini` 的 include 路径、时钟宏、monitor_speed、FreeRTOS 配置以及本课本地配置。配置错时，源码看起来对也会编译或运行失败。

## 13. 常见问题排查

### 13.1 PC13 一直灭或亮

检查 PA0 电压是否跨过阈值。
再看 `g_adc_buf` 是否变化。

### 13.2 g_adc_buf 全是 0

查 PA0 模式、ADC 时钟、ADC 启动和 DMA 请求。

### 13.3 DMA 中断不进

查 DMA1 时钟、Channel1、HTIE/TCIE、NVIC。

### 13.4 任务不醒

查 `g_adc_task_handle` 是否有效，FromISR 通知是否调用。

### 13.5 ADC 数值抖动大

增加平均长度或采样时间，检查输入源阻抗。

### 13.6 HAL 回调不进

查 `__HAL_LINKDMA` 和 HAL_DMA_IRQHandler。

### 13.7 编译失败找不到配置

查 `platformio.ini` include 路径和 FreeRTOSConfig.h。

### 13.8 进入 hook

检查任务栈和 heap。

## 14. 本课最核心的结论

1. ADC 负责采样，DMA 负责搬运，任务负责处理。
2. DMA1_Channel1 是 ADC1 固定通道。
3. 半满/全满中断只通知任务。
4. ADC 时钟必须分频到规格内。
5. 校准能减少 ADC 偏差。
6. 平均值阈值让 LED 现象稳定。
7. HAL 版关键是 handle 和 DMA 关联。
8. 排错要分 ADC、DMA、中断、任务四层。

## 15. 建议你现在怎么读这节课

先看第 5 章脑图，再读第 6 章名词。第 7、8 章要对着源码读，不要脱离代码背概念。最后用第 13 章做反向排查训练。

## 16. 扩展练习

1. 改变缓冲区长度或周期，观察现象变化。
2. 故意断开一个初始化步骤，记录失败现象。
3. 在 ISR/回调和任务中各加计数器，观察数据流。
4. 把寄存器版和 HAL 版同一动作写成对应表。
5. 增加返回值检查，让失败路径更清楚。

## 17. 下一课预告

- 上一课：[59_freertos_uart](../59_freertos_uart/README.md)
- 下一课：[61_small_system_project](../61_small_system_project/README.md)
### 12.-73 源码复盘点 -76

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-72 源码复盘点 -75

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-71 源码复盘点 -74

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-69 源码复盘点 -72

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-68 源码复盘点 -71

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-67 源码复盘点 -70

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-66 源码复盘点 -69

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-64 源码复盘点 -67

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-63 源码复盘点 -66

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-62 源码复盘点 -65

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-61 源码复盘点 -64

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-59 源码复盘点 -62

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-58 源码复盘点 -61

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-57 源码复盘点 -60

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-56 源码复盘点 -59

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-54 源码复盘点 -57

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-53 源码复盘点 -56

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-52 源码复盘点 -55

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-51 源码复盘点 -54

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-49 源码复盘点 -52

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-48 源码复盘点 -51

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-47 源码复盘点 -50

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-46 源码复盘点 -49

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-44 源码复盘点 -47

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-43 源码复盘点 -46

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-42 源码复盘点 -45

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-41 源码复盘点 -44

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-39 源码复盘点 -42

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-38 源码复盘点 -41

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-37 源码复盘点 -40

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-36 源码复盘点 -39

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-34 源码复盘点 -37

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-33 源码复盘点 -36

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-32 源码复盘点 -35

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-31 源码复盘点 -34

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-29 源码复盘点 -32

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-28 源码复盘点 -31

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-27 源码复盘点 -30

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-26 源码复盘点 -29

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-24 源码复盘点 -27

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-23 源码复盘点 -26

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-22 源码复盘点 -25

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-21 源码复盘点 -24

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-19 源码复盘点 -22

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-18 源码复盘点 -21

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-17 源码复盘点 -20

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-16 源码复盘点 -19

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-14 源码复盘点 -17

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-13 源码复盘点 -16

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-12 源码复盘点 -15

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-11 源码复盘点 -14

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-9 源码复盘点 -12

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-8 源码复盘点 -11

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-7 源码复盘点 -10

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-6 源码复盘点 -9

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-4 源码复盘点 -7

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-3 源码复盘点 -6

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-2 源码复盘点 -5

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.-1 源码复盘点 -4

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.1 源码复盘点 -2

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.2 源码复盘点 -1

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.3 源码复盘点 0

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.4 源码复盘点 1

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.6 源码复盘点 3

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.7 源码复盘点 4

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.8 源码复盘点 5

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.9 源码复盘点 6

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.11 源码复盘点 8

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.12 源码复盘点 9

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.13 源码复盘点 10

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.14 源码复盘点 11

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.16 源码复盘点 13

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.17 源码复盘点 14

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

### 12.18 源码复盘点 15

复盘时不要只看现象，要把本节源码中的全局变量、句柄、缓冲区长度、IRQ 名称和最终输出函数逐项对应。
如果其中一个名字说不出作用，就回到第 6 章重新定位它属于哪一层。

